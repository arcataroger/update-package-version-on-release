name: "Update package version on release with semver"
description: "This action syncs package.json version with latest release tag using semantic versioning (MAJOR.MINOR.PATCH)."
author: "pkgacek"

on:
  workflow_call:
    inputs:
      working_directory:
        required: false
        type: string

branding:
  icon: "arrow-up"
  color: "green"

runs:
  using: "node16"
  main: "dist/index.js"
